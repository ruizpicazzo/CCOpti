# Copy to .env.local for local dev, or set in the Vercel dashboard for prod.
# Points the frontend at the deployed backend. Defaults to localhost if unset.
VITE_API_URL=https://cardmax-api.onrender.com
